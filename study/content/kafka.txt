为什么Kafka吞吐量大，读写效率高

page cache
Kafka 将消息存储在磁盘上，并利用操作系统的页缓存（page cache）来提高访问速度。
这意味着，尽管数据实际存储在磁盘上，但频繁访问的数据会自动加载到内存中，从而提供接近内存访问速度的性能

零拷贝
Kafka 利用了零拷贝技术，即数据可以直接从磁盘缓冲区发送给网络层，而不需要先复制到应用的内存空间。这样减少了数据拷贝次数，提高了效率

顺序写入
Kafka 在写入数据时使用顺序写入的方式，这种方式比随机写入更有效率，因为顺序写入避免了寻道时间，这是磁盘 I/O 中最耗时的部分


